{
  "name": "app-de-visitas",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch --serve"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
