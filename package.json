{
  "name": "app-de-visitas",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch --serve",
    "version:bump": "node bump-version.cjs",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "google-auth-library": "^10.9.0"
  }
}
